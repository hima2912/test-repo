openapi: 3.0.0
info:
  version: '1.0'
  title: test-gem
servers:
  - url: /
paths:
  /models/{a_model_id}:batchEmbedContents:
    post:
      responses:
        '200':
          description: success
          content:
            application/json:
              schema:
                type: string
      requestBody:
        content:
          application/json:
            schema:
              type: string
      parameters:
        - name: a_model_id
          in: path
          required: true
          schema:
            type: string
          example: gemini-1.5-flash
  /models/{a_model_id}:countTokens:
    post:
      responses:
        '200':
          description: success
          content:
            application/json:
              schema:
                type: string
      requestBody:
        content:
          application/json:
            schema:
              type: string
      parameters:
        - name: a_model_id
          in: path
          required: true
          schema:
            type: string
          example: gemini-1.5-flash
  /models/{a_model_id}:embedContent:
    post:
      responses:
        '200':
          description: success
          content:
            application/json:
              schema:
                type: string
      requestBody:
        content:
          application/json:
            schema:
              type: string
      parameters:
        - name: a_model_id
          in: path
          required: true
          schema:
            type: string
          example: gemini-1.5-flash
  /models/{a_model_id}:generateContent:
    post:
      responses:
        '200':
          description: success
          content:
            application/json:
              schema:
                type: string
      requestBody:
        content:
          application/json:
            schema:
              type: string
      parameters:
        - name: a_model_id
          in: path
          required: true
          schema:
            type: string
          example: gemini-1.5-flash
  /models:
    get:
      responses:
        '200':
          description: success
          content:
            application/json:
              schema:
                type: string
  /models/{a_model_id}:
    get:
      responses:
        '200':
          description: success
          content:
            application/json:
              schema:
                type: string
      parameters:
        - name: a_model_id
          in: path
          required: true
          schema:
            type: string
          example: gemini-1.5-flash
components:
  securitySchemes:
    clientID:
      type: apiKey
      in: header
      name: X-IBM-Client-Id
security:
  - clientID: []
