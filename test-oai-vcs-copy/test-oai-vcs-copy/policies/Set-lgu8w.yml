kind: Set
apiVersion: api.ibm.com/v1
metadata:
  name: Set-lgu8w
  namespace: test-oai-vcs
  version: '1.0'
  tags: []
  labels:
    gatewayTypes:
      - nano
spec:
  valueType: jsonata
  variable:
    name: a_model_id
    value: $pathParameter('a_model_id')
